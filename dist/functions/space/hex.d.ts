import { Hex, ALL_COLORS } from '../../types';
export declare const toHexChars: (input: string) => Hex;
export declare const toHexCharsGroup: (input: string[]) => Hex[];
export declare const toHex: (input: ALL_COLORS) => Hex;
